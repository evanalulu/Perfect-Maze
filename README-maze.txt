If you write any functions that take a Maze object
as a parameter, you must pass the Maze by reference.

The Maze class doesn't have a proper copy constructor,
so passing by value doesn't work correctly.
